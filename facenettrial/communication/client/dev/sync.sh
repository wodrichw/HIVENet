#!/bin/bash
alias a='git add .  && git commit -m "m" &&  git push && git checkout master &&  git add . &&   git commit -m "m" &&  git pull &&  git checkout communication &&  git checkout master -- server.py'
